package com.asteroid.duck.pointy.indexer.scan;

import lombok.Data;

import java.nio.file.Path;

@Data
public class IndexAction {
/*
    private final ActionType type;
    private final String checksum;
    private final Path path;

    public static Action add(Path p, String checksum) {
        return new Action(ActionType.ADD, checksum, p);
    }

    public static Action update(Path p, String checksum) {
        return new Action(ActionType.UPDATE, checksum, p);
    }
    public static Action delete(Path p, String checksum) {
        return new Action(ActionType.DELETE, checksum, p);
    }
*/

}
